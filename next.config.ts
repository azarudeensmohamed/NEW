import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
   domains: ['drahomes.in', 'slelguoygbfzlpylpxfs.supabase.co','websitedemos.net','localhost:300', 'radianrise.netlify.app'],
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
};


export default nextConfig;
